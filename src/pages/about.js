import React from "react"
import Header from "../components/Header"

const about = () => {
  return (
    <div style={{ color: `teal` }}>
      <Header headerText="Brady Corps" />
      <Header headerText="About Page" />
    </div>
  )
}

export default about
